#!/bin/bash
# https://wiki.archlinux.org/index.php/feh#File_Browser_Image_Launcher
shopt -s nullglob

if [[ -z "$1" ]]; then
	FULLPATH="$(pwd)"
else
    FULLPATH=$(readlink -f "$1")
    DNAME=$(dirname "$FULLPATH")
fi

#
# TODO: remove debug
if [[ ${HOSTNAME} == 'akroshko-main' ]]; then
    # TODO: try and "maximize"
    if [[ -d "$FULLPATH" ]];then
        feh --sort filename --info "exiv2 \"%f\" 2>/dev/null | grep -v ': *$'" --action6 ";nohup gimp \"%f\" &" --geometry=1920x1080+1280+0 "$FULLPATH"
    else
        feh --sort filename --info "exiv2 \"%f\" 2>/dev/null | grep -v ': *$'" --action6 ";nohup gimp \";%f\" &" --geometry=1920x1080+1280+0 "$DNAME" --start-at "$FULLPATH"
    fi
else
    # XXXX: fullscreen causes issues on viewing laptop from pcmanfm, specicific to laptop for now
    # TODO: try and "maximize"
    if [[ -d "FULLPATH" ]]; then
        feh --sort filename --info "exiv2 \"%f\" 2>/dev/null | grep -v ': *$'" --action6 ";nohup gimp \"%f\" &" --geometry=1280x800+0+0 "$FULLPATH"
    else
        feh --sort filename --info "exiv2 \"%f\" 2>/dev/null | grep -v ': *$'" --action6 ";nohup gimp \"%f\" &" --geometry=1280x800+0+0 "$DNAME" --start-at "$FULLPATH"
    fi
fi
